# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.0
itsdangerous>=2.1.0

# Database
sqlalchemy>=2.0.0

# Auth / config
bcrypt>=4.0.0
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# LLM client (Mesh API is OpenAI-compatible, so we use the openai SDK)
openai>=1.30.0

# Vector database
chromadb>=0.5.0

# Agent framework + observability (bonuses)
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langsmith>=0.1.0

# Scheduling (bonus)
apscheduler>=3.10.0

# HTTP client (for any direct Mesh calls)
httpx>=0.27.0
